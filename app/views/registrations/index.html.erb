<div class="container my-5">
  <div class="text-center mb-4">
    <h1>Registrations</h1>
    <p>Browse event registrations in EventHub.</p>
  </div>

  <table class="table table-striped table-bordered">
    <thead>
      <tr>
        <th>User</th>
        <th>Event</th>
        <th>Status</th>
      </tr>
    </thead>

    <tbody>
      <% @registrations.each do |registration| %>
        <tr>
          <td><%= link_to registration.user.name, user_path(registration.user) %></td>
          <td><%= link_to registration.event.title, event_path(registration.event) %></td>
          <td><%= registration.status.humanize %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <div class="mt-4">
    <%= link_to "Back to Home", root_path, class: "btn", style: "background-color: #ce0615; color: white;" %>
  </div>
</div>